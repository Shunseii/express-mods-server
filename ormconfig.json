{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "postgres",
  "database": "express-mods",
  "synchronize": true,
  "logging": false,
  "entities": ["build/entities/**/*.*"],
  "migrations": ["build/migrations/**/*.*"],
  "cli": {
    "migrationsDir": "src/migrations"
  }
}
